{
  "name": "cic-paperwork",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "CIC bureaucratic intake forms — JSON-defined, procedurally styled, Netlify-hosted.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/dev-server.mjs",
    "check": "node scripts/check.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
