{
  "name": "cic-paperwork",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "CIC bureaucratic intake forms — a static GitHub Pages site. Fill in, save as PDF, receive another form.",
  "scripts": {
    "serve": "node scripts/serve.mjs",
    "check": "node scripts/check.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
